## 正态化流模型

这个变换可以将一个复杂的分布映射到一个已知的简单的分布上，并且这个映射具有可逆性，即从这个简单分布上采样一点，可以逆变换成正常样本。

公式：

$\log p_X(x)=\log p_Z\left(f_\theta(x)\right)+\log \left|\operatorname{det} \frac{\partial f_\theta(x)}{\partial x}\right|$

我们训练样本是正常图片和经过泊松编辑得到的异常图片。

我们的目标是：

对于正常图片，得到的 p_X 尽可能高；对于异常图片，得到的 p_X 尽可能低。

由于直接计算行列式计算量巨大，所以它采用了一种便于计算雅可比行列式的模型架构。

它将输入 x 分成两份, x1, x2。x1 对应的输出 y1 直接等于 x1，x2 对应的输出 y2 和 x1 有关。

x1 经过第一层会输出 s， 和 t，y2 等于 s * x2 + t  

y1，y2 再拼成 y，此时 y 对 x 的雅可比行列式直接等于 s。

为了让模型更加复杂，上面描述的网络可以继续叠加。

y 先经过特定的 shuffle，得到 y_shuffle, 再将 y_shuffle 拆分成两个部分，重复上面的过程。
